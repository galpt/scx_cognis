# Build output
/target/

# Cargo lock is committed for binary crates (do not add Cargo.lock here)

# BPF / LLVM artefacts
# NOTE: intf.h, main.bpf.c, and src/bpf.rs are committed source files.
#       They were previously generated by scx_rustland_core::RustLandBuilder
#       but now contain custom BPF code and are compiled directly by
#       scx_cargo::BpfBuilder. Do NOT add them back to this ignore list.
*.o
*.d
*.ll
*.bc

# Build directory override (set in .cargo/config.toml)
/tmp/

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store

# Environment / secrets
.env
.env.*

# Profiling artefacts
perf.data
perf.data.old
flamegraph.svg
